# --- secrets: NEVER commit ---
.runpod_key
.anthropic_key
*.key
.env
.env.*
# but DO keep the committed template (placeholders only, no secrets):
!webapp/deploy/.env.example

# --- machine/account-specific runtime state ---
.runpod_endpoint

# --- python caches ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# --- local virtualenvs ---
.venv/
venv/

# --- local experiment artifacts / stores (regenerated by runs) ---
store/
vf_runs/
vf_runs*/
vf_runs_test/
/data/
docs/EXPERIMENTS_RESULT.json
.vf_campaign_memory/

# --- agent/editor local state (not project source) ---
.claude/

# --- local scratch / session artifacts (underscore-prefixed, never source) ---
_*.py
_*.log
_*.json
_*.md
test-report.md

# --- benchmark/ablation run outputs (scripts are committed, results are not) ---
scripts/ablation_literature_results.json
scripts/bench_intelligence_results.json
scripts/scale_largedata_results.json

# --- os ---
.DS_Store

webapp/certs/*
!webapp/certs/082a08195968.json
!webapp/certs/082a08195968.events.json
